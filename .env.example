# GitHub PAT for issue-tracker operations (wayfinder map/tickets, research branch PRs)
GH_TOKEN=

# This repo's GitHub coordinates
GH_REPO=wulke/best-ball-pl

# Free-tier key from the-odds-api.com for the manual daily-slate odds pull
# (`npm run odds -- --profile <daily-profile-id>`, #79). ~500 credits/month;
# a single slate pull costs ~10.
ODDS_API_KEY=
